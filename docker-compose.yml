services:
  api:
    build:
      context: .
      target: runtime
    ports:
      - "8000:8000"
    environment:
      PRIVEIL_DEBUG: "false"

  test:
    build:
      context: .
      target: test
    command: ["pytest", "tests/", "-v"]
